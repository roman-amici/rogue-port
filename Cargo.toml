[package]
name = "rogue-port"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
bevy_ecs = "0.13.0"
sdl2 = { version = "0.36", features = ["image", "unsafe_textures", "ttf"] }
rand = "0.8"
